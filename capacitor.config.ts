import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lla.testpush',
  appName: 'PushApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
